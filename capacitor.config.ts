import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aifitnesscoach',
  appName: 'AI Fitness Coach',
  webDir: 'out',
  ios: {
    contentInset: 'automatic',
  },
};

export default config;
