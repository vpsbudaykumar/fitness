import {test,expect} from "@playwright/test";import {classifyIntent} from "@/lib/coach/intent";
test("classifies controlled coach intents",()=>{expect(classifyIntent("I only have 20 minutes")).toBe("modify_workout_duration");expect(classifyIntent("I don't have a barbell")).toBe("equipment_unavailable");expect(classifyIntent("Can I replace bench press?")).toBe("substitute_exercise");expect(classifyIntent("Make it easier")).toBe("adjust_difficulty");expect(classifyIntent("How do I bench press?")).toBe("explain_exercise");expect(classifyIntent("My knee hurts")).toBe("safety_concern");});
